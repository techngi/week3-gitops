apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "week3-app.fullname" . }}-test-connection"
  labels:
    {{- include "week3-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "week3-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
